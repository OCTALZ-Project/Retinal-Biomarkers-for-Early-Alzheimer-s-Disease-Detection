# Requirements for Fix Imbalance and Data Cleaning
# Install with: pip install -r requirements.txt
#
# --- System font for figures (Arial; NOT a pip package) ---
# Figures embed Arial as Type-42 (TrueType) for Springer/GeroScience. Install the
# Microsoft core fonts so matplotlib can find Arial; without them it falls back to
# Liberation Sans / Nimbus Sans / DejaVu Sans (still sans-serif + Type-42, just not
# literally Arial):
#     conda:          conda install -c conda-forge mscorefonts
#     Debian/Ubuntu:  sudo apt-get install ttf-mscorefonts-installer
# Then refresh matplotlib's font cache:  rm -f ~/.cache/matplotlib/fontlist-*.json

# Core data science
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3

# Machine learning
scikit-learn==1.7.2
xgboost==3.2.0
imbalanced-learn==0.14.1

# Survival analysis
lifelines==0.30.1

# Explainability
shap==0.49.1

# Visualization
matplotlib==3.10.8
seaborn==0.13.2
plotly==6.5.2

# Progress bars
tqdm==4.67.3

# --- Transitive dependencies ---
autograd==1.8.0
autograd-gamma==0.5.0
cloudpickle==3.1.2
contourpy==1.3.2
cycler==0.12.1
fonttools==4.61.1
formulaic==1.2.1
interface-meta==1.3.0
joblib==1.5.3
kiwisolver==1.4.9
llvmlite==0.46.0
narwhals==2.17.0
numba==0.64.0
nvidia-nccl-cu12==2.29.3
packaging==24.2
pillow==12.1.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
sklearn-compat==0.1.5
slicer==0.0.8
threadpoolctl==3.6.0
typing_extensions==4.15.0
tzdata==2025.3
wrapt==2.1.1
